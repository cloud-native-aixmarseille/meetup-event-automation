[codespell]
# Codespell uses English dictionaries; French catalogs are reviewed separately.
skip = *.svg,**/*.fr.ts,**/package-lock.json,**/pnpm-lock.yaml,**/node_modules/**,**/dist/**,**/coverage/**
ignore-words-list = addIn
