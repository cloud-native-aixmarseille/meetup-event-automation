export interface MessageParameters {
	"comment.agenda.item": { item: number; speaker: string };
	"comment.agenda.speaker-suffix": { speaker: number };
	"comment.agenda.speaker": { speaker: number };
}
